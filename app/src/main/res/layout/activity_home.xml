<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/home_bg"
    tools:context=".ui.HomeActivity">

    <fragment
        android:id="@+id/nav_host_fragment_activity_home"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:navGraph="@navigation/mobile_navigation" />

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottomapp_bar"
        android:layout_width="match_parent"
        android:layout_height="67dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bottomnav_bg"
        app:fabCradleMargin="10dp"
        app:fabCradleRoundedCornerRadius="10dp"
        app:fabCradleVerticalOffset="10dp"
        tools:ignore="VisualLintBottomAppBar">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:layout_width="match_parent"
            android:layout_height="67dp"
            android:id="@+id/nav_view"
            android:layout_gravity="bottom"
            android:layout_marginRight="16dp"
            android:background="@drawable/transparant_bg"
            app:itemIconTint="@drawable/buttomnav_color"
            app:itemTextColor="@drawable/buttomnav_color"
            app:itemRippleColor="@color/white"
            app:itemIconSize="30dp"
            app:menu="@menu/bottom_nav_menu"/>

    </com.google.android.material.bottomappbar.BottomAppBar>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/record"
        app:backgroundTint="@color/green"
        app:maxImageSize="40dp"
        app:srcCompat="@drawable/baseline_mic_none_72"
        app:tint="@android:color/white"
        app:layout_anchor="@id/bottomapp_bar"/>


</androidx.coordinatorlayout.widget.CoordinatorLayout>